kind = "web"
previewPath = "/"
title = "Padel Concierge"
version = "1.0.0"
id = "artifacts/padel-concierge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20441

[services.development]
run = "pnpm --filter @workspace/padel-concierge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/padel-concierge", "run", "build" ]
publicDir = "artifacts/padel-concierge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20441"
BASE_PATH = "/"
