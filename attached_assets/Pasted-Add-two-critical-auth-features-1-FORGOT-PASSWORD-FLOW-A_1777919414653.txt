Add two critical auth features:

## 1. FORGOT PASSWORD FLOW
Add a complete "Forgot Password" feature:
- On the /login page, add a "Forgot password?" link below the password field
- Create a /forgot-password page with an email input form
- When user submits email: generate a secure random token (crypto.randomBytes), save it to a password_reset_tokens table with: user_id, token, expires_at (1 hour from now)
- Send a reset email using Nodemailer with a link: https://[app-url]/reset-password?token=TOKEN
- Create a /reset-password page where user enters new password (with confirm password field)
- On submit: validate token is valid and not expired, hash new password with bcrypt, update users table, delete the used token
- Show appropriate success/error messages at each step

For email sending, use Nodemailer with SMTP. Store SMTP credentials in environment variables:
SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, SMTP_FROM

## 2. SUPER ADMIN SECRET CODE LOGIN (Creator bypass)
Add a hidden admin login method for the app creator:
- On the /login page, if user types the email "admin" and the password matches the SUPER_ADMIN_CODE environment variable, bypass normal auth and log in as a super admin
- The super admin account should have a special role: "owner" in the database
- If user with owner role doesn't exist, create it automatically on first super admin login with: email: misha.belokur@gmail.com, name: Misha (Owner), role: owner
- Set SUPER_ADMIN_CODE in the .env file — generate a strong random code like: MISHA_$(random 8 char uppercase string) and show me what it is
- Super admin sees a special golden crown badge next to their name in the UI
- Super admin can access a /admin panel showing: all users list, ability to manually set any user's skill level, ability to delete users

## 3. FIX CURRENT LOGIN
Also fix the main login issue right now:
- Check if misha.belokur@gmail.com exists in users table
- If not, create the account with password: Padel2024! (properly bcrypt hashed)
- Make sure the bcrypt compare logic in POST /api/auth/login is working correctly

After all changes, show me:
1. The SUPER_ADMIN_CODE value
2. Confirmation that misha.belokur@gmail.com account exists with password Padel2024!